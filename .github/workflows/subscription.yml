name: Build VLESS Subscription

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: TEST
        run: |
          echo "NEW WORKFLOW IS RUNNING"
          echo "================================"
          git clone --depth 1 https://github.com/VP01596/vless-top15.git source
          echo "================================"
          echo "SOURCE FILES:"
          find source -type f -name "*.txt"
